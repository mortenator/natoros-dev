# @natoros/cli

CLI for NatorOS, the agentic OS and AI OS for governed enterprise AI workers.

```bash
natoros agents list --sandbox
natoros workflows list --sandbox
natoros sandbox run wf_weekly_pipeline_summary
```
